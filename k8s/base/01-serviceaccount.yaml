# Dedicated ServiceAccount for claude-agent pod.
# Avoids using the default ServiceAccount which may have
# unintended permissions from other workloads.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: claude-agent
  namespace: default
  labels:
    app: claude-agent
automountServiceAccountToken: true
