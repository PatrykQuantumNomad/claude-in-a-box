# KIND cluster configuration for Claude In A Box local development.
# Creates a 3-node cluster: 1 control-plane + 2 workers.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: claude-in-a-box
nodes:
  - role: control-plane
  - role: worker
  - role: worker
